This is an implementation of Strassen's matrix multiplication algorithm in C, parallelized using OpenMP. 

This could be improved by implementing Morton ordered matrices. Another imporovement would be to add
matrix padding for odd-sized matrices (not N*N). 

Other than that, it is a highly-optimzed implementation. 
